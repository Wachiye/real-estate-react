import Nav from "../Nav/Nav";
const Header = () => {
    return (
        <header>
            <div className="container p-0">
                <Nav />
            </div>
        </header>
    );
}

export default Header;
